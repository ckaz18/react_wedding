import React from "react";


const Registry = () => {
    return (
        <div>
            <section className="main">
                Registry
            </section>
        </div>
    );
};

export default (Registry)